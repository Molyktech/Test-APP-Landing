export interface IFAQ {
   
        id: string;
        question: string;
        answer: string;
}

export interface ITestimonial {
    id: string;
    comment: string;
    avatarUrl: string;
    name: string;
    role: string;
}